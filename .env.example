# LLM provider: OpenAI-compatible endpoint (e.g. Ollama, vLLM)

# Base URL of the OpenAI-compatible API endpoint.
OPENAI_BASE_URL=http://100.95.26.48:1919/v1

# API key for the endpoint. Can be any string for local endpoints.
OPENAI_API_KEY=dummy

# Optional model override; falls back to the default above.
# AI_MODEL=
